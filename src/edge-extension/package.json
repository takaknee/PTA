{
  "name": "pta-edge-extension",
  "version": "1.0.0",
  "description": "PTA AI業務支援ツール Edge拡張機能",
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "build:prod": "node build.js --prod",
    "install-dompurify": "npm install dompurify && npm run copy-dompurify",
    "copy-dompurify": "cp node_modules/dompurify/dist/purify.min.js vendor/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "optionalDependencies": {
    "dompurify": "^3.0.5"
  },
  "keywords": [
    "chrome-extension",
    "edge-extension",
    "ai",
    "pta"
  ],
  "author": "Development Team",
  "license": "ISC"
}